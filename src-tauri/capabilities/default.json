{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Image Compressor main window",
  "windows": ["main"],
  "permissions": ["core:default", "core:webview:default", "dialog:default"]
}
